---
description: React Native 样式与命名规则
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# RN 样式规则

- React Native 组件样式统一使用官方 `StyleSheet` 组织。
- `StyleSheet` key 必须使用 camelCase 命名。
- 禁止在 RN 组件中使用 Web CSS 类名写法。
- 平台样式差异优先放在适配层或 `Platform.select`，避免散落在业务逻辑中。
