---
description: Jest 单元测试必跑与覆盖率要求
globs: "**/*.{ts,tsx,js,jsx}"
alwaysApply: false
---

# 测试规则

- 测试框架统一使用 `Jest`，且仅执行单元测试。
- 测试文件统一放在 `src/**/__tests__` 目录下。
- 功能改动必须新增或更新单元测试，不允许只改实现不改测试。
- 每组相关改动至少包含 3 类用例：成功路径、失败路径、边界条件。
- 覆盖率门槛为 `70%`（lines、branches、functions、statements）。
- 修复缺陷时必须先补可复现缺陷的测试，再提交修复代码。
- 提交前应执行测试命令并确认结果；若无法执行，需说明阻塞原因与替代验证。
